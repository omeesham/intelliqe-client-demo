# Test Cases

Generated by JBS IntelliQE — 4 test case(s).

| Module | Cases |
|---|---|
| Authentication | 4 |

## Authentication

### TC-001 — Verify successful login with valid credentials redirects to Dashboard
**Feature:** User Login · **Priority:** P0 · **Type:** positive · **Severity:** Critical

Confirms a registered user with valid credentials can authenticate through the standard login form and is redirected to the Dashboard with a session created. This is the primary gatekeeper flow rated High risk.

**Precondition:** A registered user account exists with username 'Admin' and password 'admin123'. The login page is reachable and the environment is qa.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: Login page loads showing the OrangeHRM logo, Username field, Password field, Login button, and 'Forgot your password?' link
2. Enter 'Admin' into the field labelled 'Username' → Expected: Username field displays 'Admin' with no validation error
3. Enter 'admin123' into the field labelled 'Password' → Expected: Password field shows masked characters with no validation error
4. Click the 'Login' button → Expected: User is authenticated and redirected to the Dashboard page within 3 seconds; the URL contains '/dashboard' and the dashboard header is visible

**Expected Result:** User is authenticated, redirected to the Dashboard, and a valid authenticated session is established.

**Tags:** smoke, regression, auth

### TC-002 — Verify login fails with valid username and invalid password showing generic error
**Feature:** User Login · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Confirms authentication is rejected when the password is wrong and a generic 'Invalid credentials' message is displayed without revealing which field was incorrect, mitigating user-enumeration risk.

**Precondition:** A registered user account exists with username 'Admin'. The login page is reachable.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: Login page is displayed
2. Enter 'Admin' into the Username field → Expected: Username field displays 'Admin'
3. Enter 'WrongPass999' into the Password field → Expected: Password field shows masked characters
4. Click the 'Login' button → Expected: An 'Invalid credentials' error message is displayed; the message does not indicate whether username or password was wrong; the user remains on the Login page

**Expected Result:** Authentication fails, a generic 'Invalid credentials' error appears, and the user stays on the Login page.

**Tags:** regression, auth, security

### TC-003 — Verify login fails with invalid username and valid password showing generic error
**Feature:** User Login · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms authentication is rejected when the username does not exist even if a valid password is supplied, and the same generic error is returned to prevent user enumeration.

**Precondition:** No account exists for username 'NonExistentUser01'. The login page is reachable.

**Steps:**

1. Navigate to the login page → Expected: Login page is displayed
2. Enter 'NonExistentUser01' into the Username field → Expected: Username field displays the entered value
3. Enter 'admin123' into the Password field → Expected: Password field shows masked characters
4. Click the 'Login' button → Expected: An 'Invalid credentials' error message is displayed identical to the wrong-password case; the user remains on the Login page

**Expected Result:** Authentication fails with the same generic 'Invalid credentials' message; user stays on the Login page.

**Tags:** regression, auth, security

### TC-010 — Verify pressing Enter with valid credentials triggers login
**Feature:** Keyboard Submission · **Priority:** P2 · **Type:** positive · **Severity:** Moderate

Confirms that pressing the Enter key after entering valid credentials submits the login form equivalently to clicking the Login button, supporting keyboard accessibility.

**Precondition:** A registered user account exists with username 'Admin' and password 'admin123'.

**Steps:**

1. Navigate to the login page → Expected: Login page is displayed
2. Enter 'Admin' into the Username field → Expected: Username field displays 'Admin'
3. Enter 'admin123' into the Password field → Expected: Password field shows masked characters
4. Press the Enter key while focus is in the Password field → Expected: The login process is triggered and the user is redirected to the Dashboard

**Expected Result:** Enter-key submission authenticates the user and redirects to the Dashboard.

**Tags:** regression, auth
