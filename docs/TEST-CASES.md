# Test Cases

Generated by JBS IntelliQE — 16 test case(s).

| Module | Cases |
|---|---|
| Authentication | 12 |
| Dashboard | 4 |

## Authentication

### TC-001 — Verify successful login with valid credentials redirects to Dashboard
**Feature:** User Login · **Priority:** P0 · **Type:** positive · **Severity:** Critical

Confirms a registered user can authenticate with a valid username and password and is redirected to the Dashboard. This is the primary gatekeeper flow; failure blocks all application access.

**Precondition:** The OrangeHRM QA instance is reachable. A registered user account exists with username 'Admin' and password 'admin123'. The login page is loaded.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login form is displayed with a Username field, a Password field, and a 'Login' button
2. Enter 'Admin' into the field labelled 'Username' → Expected: The Username field shows 'Admin' and displays no validation error
3. Enter 'admin123' into the field labelled 'Password' → Expected: The Password field shows masked characters and displays no validation error
4. Click the 'Login' button → Expected: The user is redirected to the Dashboard URL (/web/index.php/dashboard/index) and the Dashboard heading is displayed

**Expected Result:** The user is authenticated and lands on the Dashboard. A valid session is established.

**Tags:** smoke, regression, auth

### TC-002 — Verify 'Required' validation appears for both empty Username and Password on submit
**Feature:** Field Validation · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms that submitting the login form with both fields empty surfaces a 'Required' validation message beneath each field. Mitigates the risk of invalid submissions reaching the auth backend.

**Precondition:** The login page is loaded and both the Username and Password fields are empty.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login form is displayed with empty Username and Password fields
2. Leave the Username and Password fields blank and click the 'Login' button → Expected: The form does not submit and the user remains on the login page
3. Observe the message beneath the Username field → Expected: A 'Required' validation message is displayed beneath the Username field
4. Observe the message beneath the Password field → Expected: A 'Required' validation message is displayed beneath the Password field

**Expected Result:** Both fields display a 'Required' validation message and no authentication attempt is made.

**Tags:** regression, ui, data-validation

### TC-003 — Verify 'Required' validation appears for empty Password when Username is filled
**Feature:** Field Validation · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms the Password required-field rule is enforced independently when the Username is populated. Ensures login cannot proceed without a password.

**Precondition:** The login page is loaded.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login form is displayed
2. Enter 'Admin' into the field labelled 'Username' → Expected: The Username field shows 'Admin'
3. Leave the Password field empty and click the 'Login' button → Expected: The form does not submit and the user remains on the login page
4. Observe the message beneath the Password field → Expected: A 'Required' validation message is displayed beneath the Password field; no validation error appears beneath the Username field

**Expected Result:** A 'Required' message appears only beneath the Password field and authentication is not attempted.

**Tags:** regression, ui, data-validation

### TC-004 — Verify 'Required' validation appears for empty Username when Password is filled
**Feature:** Field Validation · **Priority:** P1 · **Type:** negative · **Severity:** Major

Confirms the Username required-field rule is enforced independently when the Password is populated. Ensures login cannot proceed without a username.

**Precondition:** The login page is loaded.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login form is displayed
2. Enter 'admin123' into the field labelled 'Password' → Expected: The Password field shows masked characters
3. Leave the Username field empty and click the 'Login' button → Expected: The form does not submit and the user remains on the login page
4. Observe the message beneath the Username field → Expected: A 'Required' validation message is displayed beneath the Username field; no validation error appears beneath the Password field

**Expected Result:** A 'Required' message appears only beneath the Username field and authentication is not attempted.

**Tags:** regression, ui, data-validation

### TC-005 — Verify login fails with a valid username and an incorrect password
**Feature:** User Login · **Priority:** P0 · **Type:** negative · **Severity:** Critical

Confirms an existing user with a wrong password is denied access and shown the 'Invalid credentials' error banner. Protects against unauthorized access via password guessing.

**Precondition:** A registered user account exists with username 'Admin'. The login page is loaded.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login form is displayed
2. Enter 'Admin' into the field labelled 'Username' → Expected: The Username field shows 'Admin'
3. Enter 'WrongPass999' into the field labelled 'Password' → Expected: The Password field shows masked characters
4. Click the 'Login' button → Expected: An 'Invalid credentials' error banner is displayed and the user remains on the login page

**Expected Result:** Access is denied and an 'Invalid credentials' error banner is displayed; no session is established.

**Tags:** regression, auth, security

### TC-006 — Verify login with a non-existent username returns the same generic 'Invalid credentials' error as a wrong password
**Feature:** User Login · **Priority:** P1 · **Type:** security · **Severity:** Major

Confirms that a non-existent username produces the identical generic 'Invalid credentials' message used for a wrong password, preventing username enumeration.

**Precondition:** No account exists with username 'GhostUser404'. The login page is loaded.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login form is displayed
2. Enter 'GhostUser404' into the field labelled 'Username' → Expected: The Username field shows 'GhostUser404'
3. Enter 'admin123' into the field labelled 'Password' → Expected: The Password field shows masked characters
4. Click the 'Login' button → Expected: An 'Invalid credentials' error banner is displayed
5. Compare the error text to the message shown for a wrong-password attempt (REQ-LOGIN-05) → Expected: The error message is identical and does not reveal whether the username exists

**Expected Result:** Access is denied with a generic 'Invalid credentials' banner that is indistinguishable from the wrong-password case.

**Tags:** regression, auth, security

### TC-007 — Verify username field case sensitivity on login
**Feature:** User Login · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Confirms the login backend's handling of username casing by attempting login with an altered-case username. Documents whether 'admin' is treated the same as 'Admin', which affects both valid access and enumeration behavior.

**Precondition:** A registered user account exists with the canonical username 'Admin' and password 'admin123'. The login page is loaded.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login form is displayed
2. Enter 'admin' (all lowercase) into the field labelled 'Username' → Expected: The Username field shows 'admin'
3. Enter 'admin123' into the field labelled 'Password' → Expected: The Password field shows masked characters
4. Click the 'Login' button and observe the outcome → Expected: The system behaves consistently with the documented casing rule: either the user is authenticated and redirected to the Dashboard (case-insensitive) or an 'Invalid credentials' banner is shown (case-sensitive)

**Expected Result:** Username casing is handled per the defined data rule and the result is consistent and deterministic.

**Tags:** regression, auth, data-validation

### TC-008 — Verify login handling of leading and trailing whitespace in the Username field
**Feature:** User Login · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Confirms how the login form treats surrounding whitespace in the username. Determines whether the input is trimmed before authentication, avoiding false failures for valid users.

**Precondition:** A registered user account exists with username 'Admin' and password 'admin123'. The login page is loaded.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login form is displayed
2. Enter '  Admin  ' (with two leading and two trailing spaces) into the field labelled 'Username' → Expected: The Username field contains the value with surrounding spaces
3. Enter 'admin123' into the field labelled 'Password' → Expected: The Password field shows masked characters
4. Click the 'Login' button and observe the outcome → Expected: The system behaves consistently with the documented whitespace rule: either the value is trimmed and the user is redirected to the Dashboard, or an 'Invalid credentials' banner is shown

**Expected Result:** Whitespace around the username is handled deterministically per the defined data rule.

**Tags:** regression, auth, data-validation

### TC-009 — Verify password is not exposed in the URL or query string during login submission
**Feature:** User Login · **Priority:** P1 · **Type:** security · **Severity:** Major

Confirms the login form submits credentials over HTTPS via POST and never places the password in the URL or query string, preventing credential leakage through history or logs.

**Precondition:** The login page is loaded over HTTPS. Browser developer tools Network panel is available.

**Steps:**

1. Open browser developer tools and select the Network tab, then navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login page loads over HTTPS and network requests are being captured
2. Enter 'Admin' into the Username field and 'admin123' into the Password field, then click 'Login' → Expected: The login request is submitted
3. Inspect the login request in the Network panel → Expected: The credentials are sent via an HTTPS POST request body; the password does not appear in the request URL or any query string parameter
4. Inspect the resulting browser address bar and page URL after login → Expected: The password value is not present anywhere in the URL

**Expected Result:** Credentials are transmitted securely over HTTPS in the request body with no password exposure in URLs or query strings.

**Tags:** regression, auth, security

### TC-010 — Verify rapid double-click on Login does not create duplicate sessions or errors
**Feature:** User Login · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Confirms that double-clicking or rapidly re-submitting the Login button before redirect completes results in a single clean authentication and does not produce duplicate submissions or error states.

**Precondition:** A registered user account exists with username 'Admin' and password 'admin123'. The login page is loaded.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login form is displayed
2. Enter 'Admin' into the Username field and 'admin123' into the Password field → Expected: Both fields are populated
3. Rapidly click the 'Login' button twice in quick succession before the redirect completes → Expected: The form processes a single authentication request without throwing a duplicate-submission or server error
4. Observe the resulting page → Expected: The user is redirected to the Dashboard exactly once and the Dashboard renders normally

**Expected Result:** Rapid re-submission results in a single successful login and a single redirect to the Dashboard with no errors.

**Tags:** regression, auth

### TC-011 — Verify 'Forgot your password?' link navigates to the password reset page
**Feature:** Forgot Password · **Priority:** P1 · **Type:** positive · **Severity:** Major

Confirms the account recovery entry point works by navigating the user from the login page to the password reset page. Protects the account recovery path.

**Precondition:** The login page is loaded.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login form is displayed with a 'Forgot your password?' link visible
2. Click the 'Forgot your password?' link → Expected: The user is navigated to the password reset page (URL contains /auth/requestPasswordResetCode)
3. Observe the password reset page → Expected: The reset page displays a Username field and 'Reset Password' and 'Cancel' actions

**Expected Result:** The user is taken to the password reset page with the reset form displayed.

**Tags:** regression, auth, ui

### TC-016 — Verify end-to-end journey from login through Dashboard basic information view
**Feature:** User Login · **Priority:** P0 · **Type:** e2e · **Severity:** Critical

Validates the full primary user journey: a registered user logs in, is redirected to the Dashboard, and the basic information widgets are viewable. Covers the highest-value happy path across two modules.

**Precondition:** A registered user account exists with username 'Admin' and password 'admin123'. No active session exists at the start.

**Steps:**

1. Navigate to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login form is displayed
2. Enter 'Admin' into the Username field and 'admin123' into the Password field → Expected: Both fields are populated with no validation errors
3. Click the 'Login' button → Expected: The user is authenticated and redirected to the Dashboard (URL contains /dashboard/index)
4. Observe the Dashboard basic information widgets → Expected: The Dashboard widgets render with their titles and content, confirming the post-login landing view is functional

**Expected Result:** The user completes login and successfully views the Dashboard basic information widgets in a single uninterrupted flow.

**Tags:** smoke, e2e, auth

## Dashboard

### TC-012 — Verify Dashboard basic information widgets render after successful login
**Feature:** Dashboard Display · **Priority:** P1 · **Type:** positive · **Severity:** Major

Confirms that an authenticated user sees the Dashboard basic information widgets rendered correctly on the post-login landing view. Mitigates the risk of a broken post-authentication experience.

**Precondition:** A registered user account exists with username 'Admin' and password 'admin123'. The user has just authenticated successfully.

**Steps:**

1. Log in at https://opensource-demo.orangehrmlive.com/web/index.php/auth/login with username 'Admin' and password 'admin123' → Expected: The user is redirected to the Dashboard URL
2. Observe the Dashboard heading and page structure → Expected: The Dashboard heading is displayed and the widget grid area is present
3. Observe the basic information widgets on the Dashboard → Expected: The dashboard widgets (for example 'Time at Work', 'My Actions', 'Quick Launch') render with their titles and content without layout errors

**Expected Result:** The Dashboard loads with its basic information widgets rendered correctly for the authenticated user.

**Tags:** smoke, regression, ui

### TC-013 — Verify unauthenticated direct access to Dashboard URL redirects to login
**Feature:** Dashboard Display · **Priority:** P0 · **Type:** security · **Severity:** Critical

Confirms that an unauthenticated user attempting to open the Dashboard URL directly is redirected to the login page rather than shown any widget data. Protects against unauthorized access to post-login content.

**Precondition:** No active session exists in the browser (all cookies cleared / logged out).

**Steps:**

1. Ensure no active session by clearing cookies or opening a fresh private browsing window → Expected: No authenticated session is present
2. Navigate directly to https://opensource-demo.orangehrmlive.com/web/index.php/dashboard/index → Expected: The browser does not display Dashboard widgets
3. Observe the resulting page → Expected: The user is redirected to the login page (URL contains /auth/login) and the login form is displayed

**Expected Result:** Direct access to the Dashboard without a session redirects to the login page and exposes no dashboard content.

**Tags:** regression, auth, security

### TC-014 — Verify already-authenticated user visiting the login URL is routed to the Dashboard
**Feature:** Dashboard Display · **Priority:** P2 · **Type:** edge · **Severity:** Moderate

Confirms that an authenticated user who navigates directly to the login URL is not shown the login form again but is routed to the Dashboard, preventing session/redirect inconsistencies.

**Precondition:** A registered user is currently authenticated with an active session (logged in as 'Admin').

**Steps:**

1. Log in at https://opensource-demo.orangehrmlive.com/web/index.php/auth/login and confirm the Dashboard is displayed → Expected: An active authenticated session exists and the Dashboard is shown
2. In the same browser tab, navigate directly to https://opensource-demo.orangehrmlive.com/web/index.php/auth/login → Expected: The login form is not re-presented as an active form for an authenticated user
3. Observe the resulting page → Expected: The user is routed to the Dashboard (URL contains /dashboard/index) using the existing session

**Expected Result:** An authenticated user hitting the login URL lands on the Dashboard rather than being asked to log in again.

**Tags:** regression, auth

### TC-015 — Verify Dashboard action after session expiry redirects to login
**Feature:** Dashboard Display · **Priority:** P1 · **Type:** edge · **Severity:** Major

Confirms that when a session expires while the Dashboard is open, the next user action redirects to the login page instead of executing against an invalid session. Protects against stale-session data exposure.

**Precondition:** A registered user is authenticated and viewing the Dashboard. The server session can be invalidated (via timeout or by clearing the session cookie).

**Steps:**

1. Log in as 'Admin' and remain on the Dashboard → Expected: The Dashboard is displayed with an active session
2. Invalidate the session by deleting the session cookie in developer tools (simulating session expiry) → Expected: The session is no longer valid on the server
3. Perform a Dashboard action that triggers a server request, such as clicking a navigation menu item → Expected: The request is not served with dashboard content
4. Observe the resulting page → Expected: The user is redirected to the login page (URL contains /auth/login)

**Expected Result:** An action taken after session expiry redirects the user to the login page rather than serving protected content.

**Tags:** regression, auth, security
